{% extends 'base.html.twig' %}

{% block title %}Pvinternes{% endblock %}

{% block body %}
    <h1>Pvinternes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pvinterne.id }}</td>
            </tr>
            <tr>
                <th>Isvalidate</th>
                <td>{{ pvinterne.isvalidate ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Pourcentage</th>
                <td>{{ pvinterne.pourcentage }}</td>
            </tr>
            <tr>
                <th>Ismodified</th>
                <td>{{ pvinterne.ismodified ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pvinternes_index') }}">back to list</a>

    <a href="{{ path('pvinternes_edit', {'id': pvinterne.id}) }}">edit</a>

    {{ include('pvinternes/_delete_form.html.twig') }}
{% endblock %}
