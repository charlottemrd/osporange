<form method="post" action="{{ path('pvinternes_delete', {'id': pvinterne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pvinterne.id) }}">
    <button class="btn">Delete</button>
</form>
